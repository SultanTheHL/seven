version: "3.8"

services:
  backend:
    build: .
    container_name: car-reco-backend
    ports:
      - "8080:8080"
    environment:
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      WEATHER_API_KEY: ${WEATHER_API_KEY:-}